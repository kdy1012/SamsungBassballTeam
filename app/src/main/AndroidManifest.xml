<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="kr.co.tjeit.samsungbassballteam">

    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".SearchIDActivity" />
        <activity android:name=".SearchPWActivity" />
        <activity android:name=".SeasonSignUpActivity" />
        <activity android:name=".MoreSeeActivity" />
        <activity android:name=".MemberShipActivity" />
        <activity android:name=".fragment.FragmentMemberInfo" />
        <activity android:name=".FamilyShopActivity" />
        <activity android:name=".TodayGameActivity"></activity>
    </application>

</manifest>